---
name: equity-ticket
description: 处理会员权益相关诉求，基于会员、积分与工单接口完成查询、确认、创建与追踪。
---

# 会员权益工单

## 字段来源

- 所有请求参数、返回字段、字段命名与字段结构，以当前可用 tool 的定义和 API 文档为唯一来源。
- 本 skill 不重复维护字段词典；规划和执行时需要先查对应 tool 的说明，再决定可引用字段。
- 若后续步骤需要复用前序工具结果，应沿用 tool 中已有字段名，不要自行改名或发明同义字段。

## 使用目标

用于以下场景：

- 查询积分与可用权益
- 会员升级与升级条件说明
- 创建权益类工单并跟踪进度

## 核心规则

1. 写操作（升级/创建工单）前必须确认。
2. `user_id` 不作为工具入参显式传递，由鉴权上下文获取。
3. 避免直接询问内部术语，用用户可理解话术表达。
4. 查询信息展示遵循最小必要原则：只展示与当前问题相关字段，敏感信息脱敏。
5. 工具失败或无权限时如实说明，不编造结果。

## 处理流程

1. 识别诉求：积分兑换 / 会员升级 / 工单进度查询。
2. 先查后问：
   - 积分场景先查 `get_user_score`
   - 升级场景先查 `get_user_level`（含下一等级条件）
3. 需要写操作时先确认关键项（目标权益/目标等级/原因）。
4. 若用户具备相应条件，创建工单，否则不予操作。
5. 返回处理结果与后续查询方式。

## 工具使用说明

- 字段名、请求参数和返回结构全部以 tool 定义为准；本 skill 只说明使用时机，不重复列字段。
- `get_user_score`：查询积分、积分明细或可用额度时使用。
- `get_user_level`：查询当前等级、下一等级目标或升级条件时使用。
- `create_ticket`：需要创建权益类工单时使用。
- `get_ticket`：已有工单号，查询单个权益工单进度时使用。
- `get_tickets`：未提供工单号，需要先查工单候选列表时使用。

补充约束：

- 升级或创建工单前必须先确认目标等级、目标权益或具体原因。
- 如果查询结果已足够回答用户，不要机械进入写操作。

## 规划要点

1. 先判断是积分/权益查询、会员升级，还是权益工单进度查询。
2. 查询类优先 `get_user_score` / `get_user_level` / `get_ticket` / `get_tickets`，不要过早规划写操作。
3. 升级或创建权益工单前必须先 `confirm` 目标等级、目标权益或原因。
4. 如果当前目标更适合直接解释规则或展示查询结果，优先 `show_info`，不要机械创建工单。

## 查询与隐私

1. 仅返回当前用户本人相关信息；疑似越权时拒绝并提示“无法查询该信息”。
2. 展示时默认脱敏（手机号、地址、邮箱、证件信息等）。
3. 若工具查询失败，统一合规话术：
   “无法查询到相关信息，请稍后重试；如需继续处理，请补充必要信息。”
